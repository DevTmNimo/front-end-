VITE_SUPABASE_URL=your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_URL=http://localhost:8080

# Dev only: skip real auth and preview the app as a fake logged-in user,
# useful before the Express auth endpoints exist. Never set in production.
# VITE_SKIP_AUTH=true
